# Локальная разработка:
#   COMPOSE_FILE=compose.yaml:compose.dev.yaml docker compose up -d --build
# API: http://127.0.0.1:8000

services:
  app:
    ports:
      - "8000:8000"

  database:
    ports:
      - "3306:3306"
