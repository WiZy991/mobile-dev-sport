<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Правовая информация{% endblock %} — WorldCashFit</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #fafaf7;
            --text: #15181f;
            --muted: #6b7180;
            --primary: #ff5b2e;
            --border: #e5e2da;
        }

        * { box-sizing: border-box; }

        body {
            margin: 0;
            font-family: "Manrope", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Arial, sans-serif;
            color: var(--text);
            background: var(--bg);
            line-height: 1.65;
            -webkit-font-smoothing: antialiased;
        }

        a { color: var(--primary); text-decoration: none; }
        a:hover { text-decoration: underline; }

        .header {
            border-bottom: 1px solid var(--border);
            background: #fff;
        }

        .header-inner {
            max-width: 900px;
            margin: 0 auto;
            padding: 1rem 1.25rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .logo {
            font-weight: 800;
            color: var(--text);
        }

        .nav {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            font-size: 0.92rem;
        }

        .nav a { color: var(--muted); }

        main {
            max-width: 900px;
            margin: 0 auto;
            padding: 2rem 1.25rem 3rem;
        }

        h1 {
            font-size: clamp(1.5rem, 3vw, 2rem);
            line-height: 1.2;
            margin: 0 0 1rem;
        }

        .meta {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
            margin-bottom: 2rem;
            font-size: 0.92rem;
            color: var(--muted);
        }

        .download-btn {
            display: inline-flex;
            align-items: center;
            gap: 0.4rem;
            padding: 0.55rem 1rem;
            border-radius: 999px;
            border: 1px solid var(--border);
            background: #fff;
            color: var(--text);
            font-size: 0.92rem;
            font-weight: 600;
        }

        .download-btn:hover {
            border-color: var(--primary);
            text-decoration: none;
        }

        .content {
            background: #fff;
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 1.75rem 1.35rem 2rem;
        }

        .content h2,
        .doc-section-title {
            font-size: 1.2rem;
            margin: 0 0 0.35rem;
            line-height: 1.35;
            font-weight: 800;
            color: var(--text);
        }

        .doc-rule {
            height: 3px;
            width: 72px;
            background: var(--primary);
            border-radius: 2px;
            margin: 0 0 1.15rem;
        }

        .doc-cover {
            margin-bottom: 2rem;
            padding-bottom: 0.5rem;
        }

        .doc-subtitle {
            margin: 0 0 0.85rem;
            font-size: 1.05rem;
            font-weight: 700;
            line-height: 1.4;
        }

        .doc-city {
            margin: 1rem 0 0;
            color: var(--muted);
            font-size: 0.92rem;
        }

        .doc-meta {
            margin: 0;
            display: grid;
            grid-template-columns: minmax(9rem, 12rem) 1fr;
            gap: 0.55rem 1rem;
        }

        .doc-meta dt {
            margin: 0;
            color: var(--muted);
            font-size: 0.88rem;
            font-weight: 600;
        }

        .doc-meta dd {
            margin: 0;
            font-size: 0.95rem;
        }

        .doc-toc {
            margin-bottom: 2rem;
        }

        .doc-toc ol {
            margin: 0;
            padding-left: 1.25rem;
            display: grid;
            gap: 0.35rem;
        }

        .doc-toc a {
            color: var(--text);
            font-weight: 600;
        }

        .doc-toc a:hover {
            color: var(--primary);
        }

        .doc-section {
            margin: 2rem 0 0;
            padding-top: 0.25rem;
        }

        .doc-subheading {
            font-size: 1.02rem;
            font-weight: 750;
            margin: 1.35rem 0 0.65rem;
            line-height: 1.35;
        }

        .doc-p {
            margin: 0 0 0.9em;
            text-align: justify;
            text-indent: 1.5em; /* красная строка */
            hyphens: auto;
        }

        .doc-clause {
            margin: 0 0 0.9em;
            text-align: justify;
            text-indent: 1.5em;
            hyphens: auto;
        }

        .doc-clause .doc-num,
        .doc-subheading .doc-num {
            font-weight: 800;
            color: var(--primary);
        }

        .doc-num {
            font-weight: 800;
            color: var(--primary);
        }

        /* Сноски / термины — как боковые пояснения в PDF */
        .doc-footnotes {
            margin: 1rem 0 0;
            padding: 0;
        }

        .doc-footnotes dt {
            margin: 1rem 0 0.35rem;
            padding-left: 0.85rem;
            border-left: 3px solid var(--primary);
            font-weight: 800;
            font-size: 0.98rem;
            color: var(--text);
        }

        .doc-footnotes dt:first-child {
            margin-top: 0.35rem;
        }

        .doc-footnotes dd {
            margin: 0 0 0.35rem;
            padding-left: 0.85rem;
            border-left: 3px solid rgba(255, 91, 46, 0.22);
            color: var(--text);
            text-align: justify;
            font-size: 0.95rem;
        }

        .doc-list-bullets {
            margin: 0.35rem 0 1rem;
            padding-left: 1.6rem;
            list-style: disc;
        }

        .doc-list-bullets li {
            margin: 0.3rem 0;
            text-align: justify;
        }

        .content p {
            margin: 0 0 0.85em;
        }

        .doc-list {
            list-style: none;
            padding: 0;
            margin: 0;
            display: grid;
            gap: 0.75rem;
        }

        .doc-list a {
            display: block;
            padding: 1rem 1.1rem;
            border: 1px solid var(--border);
            border-radius: 12px;
            background: #fff;
            color: var(--text);
            font-weight: 600;
        }

        .doc-list a:hover {
            border-color: var(--primary);
            text-decoration: none;
        }

        footer {
            border-top: 1px solid var(--border);
            padding: 1.25rem;
            text-align: center;
            color: var(--muted);
            font-size: 0.88rem;
        }

        .cookie-consent-banner {
            position: fixed;
            left: 0;
            right: 0;
            bottom: 0;
            z-index: 1000;
            background: rgba(21, 24, 31, 0.97);
            color: #fff;
            padding: 12px 16px;
            border-top: 1px solid rgba(255, 255, 255, 0.15);
        }

        .cookie-consent-content {
            max-width: 980px;
            margin: 0 auto;
            display: flex;
            gap: 12px;
            align-items: center;
            justify-content: space-between;
            flex-wrap: wrap;
        }

        .cookie-consent-content p {
            margin: 0;
            font-size: 0.88rem;
            line-height: 1.45;
            color: rgba(255, 255, 255, 0.92);
            max-width: 760px;
        }

        .cookie-consent-content a {
            color: #ffcb45;
            text-decoration: underline;
        }

        .cookie-consent-content button {
            border: none;
            border-radius: 10px;
            background: #ff5b2e;
            color: #fff;
            font-weight: 700;
            padding: 10px 16px;
            cursor: pointer;
        }
    </style>
</head>
<body>
<header class="header">
    <div class="header-inner">
        <a href="{{ path('landing_page') }}" class="logo">WorldCashFit</a>
        <nav class="nav">
            <a href="{{ path('legal_index') }}">Все документы</a>
            <a href="{{ path('legal_requisites') }}">Реквизиты</a>
            <a href="{{ path('legal_privacy') }}">Конфиденциальность</a>
            <a href="{{ path('legal_license_agreement') }}">Оферта</a>
            <a href="{{ path('legal_user_agreement') }}">Пользовательское соглашение</a>
        </nav>
    </div>
</header>

<main>
    {% block body %}{% endblock %}
</main>

<footer>
    © {{ "now"|date("Y") }} WorldCashFit · <a href="{{ path('legal_index') }}">Правовая информация</a>
</footer>
{% include 'partials/cookie_banner.html.twig' %}
</body>
</html>
