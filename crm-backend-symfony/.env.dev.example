# Локальные переопределения для APP_ENV=dev (необязательно).
# Скопируйте при необходимости: cp .env.dev.example .env.dev
# Docker Compose читает только .env — секреты держите там.

###> symfony/framework-bundle ###
APP_SECRET=change_me_generate_random_string
###< symfony/framework-bundle ###
