# TLS-terminator → PHP-FPM в контейнере app:9000.
# Замените server_name на свой домен (должен совпадать с CN/SAN сертификата).
#
# resolver 127.0.0.11 — DNS Docker резолвится в runtime,
# а не при старте nginx (иначе «host not found in upstream», если app ещё не поднялся).

server {
    listen 80;
    listen [::]:80;
    server_name worldcashfit.ru www.worldcashfit.ru dobrozal.ru www.dobrozal.ru;

    # Let's Encrypt (certbot --webroot)
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name worldcashfit.ru www.worldcashfit.ru dobrozal.ru www.dobrozal.ru;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    client_max_body_size 32m;

    root /var/www/crm/public;
    index index.php;

    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ ^/index\.php(/|$) {
        resolver 127.0.0.11 valid=10s ipv6=off;
        set $crm_fastcgi app:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /app/public/index.php;
        fastcgi_param DOCUMENT_ROOT /app/public;
        fastcgi_param HTTPS on;
        fastcgi_param HTTP_PROXY "";
        fastcgi_pass $crm_fastcgi;
        fastcgi_read_timeout 70s;
        fastcgi_buffers 16 16k;
        fastcgi_buffer_size 32k;
    }

    location ~ \.php$ {
        return 404;
    }
}
